Khi tôi gửi cho bạn một bảng dữ liệu, bạn hãy thực hiện các yêu cầu sau:

1. Tạo một interface TypeScript với tên dạng I{{object_name}}ViewModel.

   * Nếu có trường id thì đổi tên thành code.
   * Tất cả các thuộc tính đều phải có dấu ?.
   * Sau mỗi thuộc tính phải có comment // chứa tên cột chính xác 100% như trong hình.

2. Tạo một mảng mock data với tên const mockData{{object_name}}: I{{object_name}}ViewModel[].

   * Mock data phải lấy 100% dữ liệu từ hình ảnh gốc.

   * Không cần comment.
   * Các trường ngày tháng (ví dụ: "Ngày cập nhật") phải được chuyển sang định dạng yyyy-mm-dd.

Here is the OCR text from the image:
{{ocr_text}}