Khi tôi gửi cho bạn một hình ảnh chụp bảng dữ liệu và tên object (ví dụ: TrainingProgram), bạn hãy thực hiện các yêu cầu sau:

Hãy chuẩn hóa nội dung bảng dữ liệu cho đúng chính tả, đúng ngữ cảnh, hợp lý

1. Tạo một export interface TypeScript với tên dạng I{{object_name}}ViewModel. Ví dụ: ITrainingProgramViewModel.
   * Các thuộc tính phải là tiếng anh
   * Các thuộc tính có kiểu dữ liệu phù hợp.
   * Nếu có trường id thì đổi tên thành code.
   * Tất cả các thuộc tính đều phải có dấu `?`.
   * Sau mỗi thuộc tính phải có comment // chứa tên cột chính xác 100% như trong hình.

2. Tạo một mảng export mock data với tên const {{object_name_plural}}: I{{object_name}}ViewModel[]. Ví dụ: const trainingPrograms: ITrainingProgramViewModel[].
   * Mock data phải lấy 100% dữ liệu từ hình ảnh gốc.
   * Không cần comment.
   * Các nội dung không có giá trị thì sử dụng undefined
   * Các trường ngày tháng (ví dụ: "Ngày cập nhật") phải được chuyển sang định dạng yyyy-mm-dd.

Nội dung trả về chỉ bao gồm interface và mockData, không giải thích


